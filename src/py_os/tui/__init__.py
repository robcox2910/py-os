"""TUI dashboard — live Textual-based terminal interface for PyOS.

Requires the ``[tui]`` optional extra (``textual>=0.90``).  Launch via
the ``pyos-tui`` entry point or ``python -m py_os.tui.app``.
"""
